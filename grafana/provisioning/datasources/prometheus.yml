# ================================
# Grafana Datasource - Prometheus
# ================================
# Configuration automatique de la source de données

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: "5s"
